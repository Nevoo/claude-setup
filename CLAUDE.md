# Global Claude Instructions

## qmd (if a project uses it)
qmd MCP tools are deferred. Load with `ToolSearch query: "qmd status"` then call `mcp__qmd__status` to verify the index. Use `mcp__qmd__search` (BM25) and `mcp__qmd__vsearch` (semantic) for project content. Don't guess about indexed content when qmd is available.

## Voice DNA
For any user-facing writing — posts, articles, scripts, marketing copy — apply the rules in `~/.claude/projects/-Users-rouven-Documents-Obsidian-idearlou/memory/voice-dna.md`. Hard rules: short paragraphs, contractions, no em dashes, no "This isn't X. This is Y." negation-then-assertion pattern, no AI clichés ("dive into", "unlock", "leverage", "in today's…", etc.).
